apiVersion: v2
name: cluster-scaler
description: A Helm chart that listen for UPS status and acts accordingly to the state

home: https://gitlab-internal.spirit-dev.net/infrastructure/helm/cluster-scaler
sources:
  - https://gitlab-internal.spirit-dev.net/infrastructure/helm/cluster-scaler
  - https://gitlab-internal.spirit-dev.net/scripts/scale-controller
  - https://gitlab-internal.spirit-dev.net/scripts/cluster-management

type: application

# Note: This is a custom internal chart without external dependencies
version: 1.0.8

# Note: This is a custom internal application
appVersion: "1.0.0"

keywords:
  - ups
  - monitoring
  - discord
